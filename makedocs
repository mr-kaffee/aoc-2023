#!/bin/bash

# remove generated files
rm -rf site/gen/*

# generate ADOC files
python3 gen_adocs.py

# convert ADOC files to HTML site
asciidoctor -a toc=right -a source-highlighter=rouge -D site/gen/out site/gen/adoc/*.adoc

# copy static content
shopt -s dotglob
cp -r site/static/* site/gen/out
